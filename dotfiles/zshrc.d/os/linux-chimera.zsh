# chimera uses freebsd userspace, so this is based on freebsd.zsh
export LSCOLORS='gxfxcxdxbxegedabagacad'
export BLOCKSIZE=K;

# enable colors in grep
export GREP_OPTIONS='--color=auto'

alias psf='ps wwfaux'
alias ls='ls -p'
